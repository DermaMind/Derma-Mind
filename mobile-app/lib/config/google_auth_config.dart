/// Google OAuth Web Client ID (from Google Cloud Console).
/// Required on Android to obtain an idToken for backend verification.
/// Must match the client ID configured on the DermaMind API.
class GoogleAuthConfig {
  static const String serverClientId =
      '81157765082-f91sd3ld0pij8btnrk36ichplpm3vhto.apps.googleusercontent.com';
}
